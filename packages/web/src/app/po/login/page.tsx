import { LoginClient } from './LoginClient';

export const metadata = {
  title: 'Case Manager Sign In — REENTRY',
  description: 'Parole and probation officer portal.',
};

export default function POLoginPage() {
  return <LoginClient />;
}
