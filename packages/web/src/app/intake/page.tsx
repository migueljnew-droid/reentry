import IntakeClient from './IntakeClient';

export const metadata = {
  title: 'Intake — REENTRY',
  description: 'Voice-first intake for returning citizens.',
};

export default function IntakePage() {
  return <IntakeClient />;
}
